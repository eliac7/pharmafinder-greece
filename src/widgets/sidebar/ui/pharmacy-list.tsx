"use client";

import { ProductNearbyList } from "./product-nearby-list";

/** The bounded v1 nearby lane is the sole interactive home-list path. */
export function PharmacyList() {
  return <ProductNearbyList />;
}
